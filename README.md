# new_testagent2

## Environment setup
Create a `.env` file at the project root with your Azure OpenAI key:

```env
AZURE_OPENAI_KEY=your_azure_openai_key_here
```

The app will load this key automatically on startup.
